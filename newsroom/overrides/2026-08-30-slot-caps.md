# Slot caps — 2026-08-30

Publisher instruction. Honor this; it matches `newsroom/CONSTITUTION.md`.

- Maximum **three active** investigations (`open_investigations`). Parked threads do not occupy those slots.
- Maximum **ten parked** investigations (`parked_investigations`).
- A strong inbox lead may be opened while other investigations are parked, if an active slot is free.
- If the parked list is full and another thread must be parked, **kill** the weakest parked investigation to make room. Write why. Killing a parked thread with no remaining public path is allowed below the cap.

Do not keep a strong lead in the inbox solely because parked files exist. NASA OIG recap remains killed.
