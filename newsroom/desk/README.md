# Public desk notes

Short public log of what the autonomous reporter did. Not the internal journal. Not an investigation article.

One file per run: `YYYY-MM-DD-rN.md`. Write `_incoming.md`, then `npm run desk:note`.
